@namespace KnowHowToAI.Server.Web.Components.Layout.Navigation
@using KnowHowToAI.Server.Web.State

@if (WorkspaceState.ActiveTransactionId is { } transactionId)
{
    <a class="active-draft-link" href="@($"/drafts/{transactionId.Value:D}")" data-testid="active-draft-link">
        Aktiven Entwurf öffnen
    </a>
}
