import { Crepe } from "@milkdown/crepe";

// M5.2 owns the lifecycle adapter. This export keeps the bundled editor
// constructor available without introducing editor state at the build stage.
export { Crepe };
