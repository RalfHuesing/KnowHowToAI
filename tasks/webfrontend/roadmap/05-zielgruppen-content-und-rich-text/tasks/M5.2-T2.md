# M5.2-T2 – Golden Master, Paste und Sicherheits-Roundtrip abnehmen

[Milestone-Roadmap](../roadmap.md) · [Content und Assets](../../../konzept/03-content-und-assets.md)

## Intention

Die kanonische Markdown-Grenze wird mit versionierten, lizenzierten Golden-Master-Fixtures und Sicherheits-/Paste-Fällen regressionssicher belegt.

## Voraussetzungen und Referenzen

- M5.1-T2 und M5.2-T1 abgenommen; Policycodes sind implementiert.
- Ignorierte M0-Fixtures nicht kopieren; Herkunft und Lizenz der neuen versionierten Fixture dokumentieren.

## Scope

- Golden Master für Absätze, Hervorhebungen, Links, Listen, Tabellen, Inline-/Fenced-Code, Blockquotes und Unicode neu versionieren.
- Fünf `Markdown → Editor → Markdown`-Zyklen semantisch gegen Markdig prüfen, inklusive Whitespace/Escaping und ~4-KiB-Inhalt.
- Plain-, Browser- und Office-Paste gemäß Policy bewusst reduzieren und jede Reduktion über `role=status` anzeigen.
- Direkt eingegebenes oder geladenes Markdown mit Raw HTML, Headings, Front Matter, unsicheren Links oder externen Bildern serverseitig ablehnen und nicht still verändern; externe Bilder laden nie.

## Nicht-Ziele

- Keine neue Editorbibliothek, keine Bilduploads und kein separater TODO-Workflow.

## Verträge und Invarianten

- **Verbindlich:** Zulässiger Markdown bleibt Markdig-semantisch äquivalent.
- **Verbindlich:** Serverablehnung überschreibt keinen ungespeicherten Editorwert.
- **Empfohlen:** Testdaten parametrisiert und deterministisch halten.

## Umsetzungsvorschlag

Fixture, semantischen Vergleich und Browserpaste-Smokes getrennt halten; bekannte Normalisierungen explizit dokumentieren.

## Akzeptanz und Testbudget

- [x] Der versionierte Golden Master umfasst alle freigegebenen Markdown-Konstrukte; der Core-Test prüft die Markdig-Baseline und der echte Playwright-/Crepe-Smoke besteht fünf aufeinanderfolgende `Markdown → Editor → getMarkdown()`-Zyklen ohne semantische Drift.
- [x] Die bUnit-Abnahme belegt, dass serverseitige Ablehnung den vollständigen ungespeicherten Editorwert, Dirty-State und die Editorinstanz erhält.
- [x] Browser-/Office-Paste reduziert unsichere Links, Headings, unbekannte Elemente und Bilder; die Vitest-Policy-Prüfung belegt, dass keine externe Bildquelle geladen wird.
- [x] Paste-Reduktionen zeigen einen zusammengefassten `role=status`-Hinweis und markieren den Inhalt als ungespeichert.
- [x] Fixture-Herkunft und Lizenz sind als eigene, projektinterne MIT-Testfixture ohne externe Quelle versioniert; Testnachweise sind unten dokumentiert.
- Nicht erforderlich: vollständige Browsermatrix oder globale Lasttests.

## Checkliste

- [x] Voraussetzungen und aktuellen Ist-Stand geprüft
- [x] Scope umgesetzt, Nicht-Ziele eingehalten
- [x] Akzeptanzkriterien und Testbudget erfüllt
- [x] Dokumentation aktualisiert und Roadmap-Checkbox erst nach Abnahme geschlossen
- [x] Abschlussnachweis und atomarer Commit erstellt

## Abschlussnachweis

- Geänderte Dateien: `ContentEditor.razor.js`, `ContentEditor.razor`, `ContentEditor.razor.cs`, Vitest-/bUnit-/Browser-Tests, `GoldenMaster.md`, `docs/Architektur.md`, diese Leaf-Datei und `roadmap.md`.
- Nachweise: `npm test -- --run` (5 Vitest-Tests), gezielte Core- und Web-bUnit-Tests (1 beziehungsweise 5 bestanden), Playwright-Smokes (Paste und echter Crepe-Roundtrip, 2 bestanden; Roundtrip fünf Zyklen). Abschluss-Gates: `scripts/build.ps1`, `scripts/test-fast.ps1` (6+416+276+371 Tests bestanden), `scripts/test-integration.ps1 -Filter 'FullyQualifiedName~ContentEditorPasteSmokeTests'` (2 bestanden) und AiNetLinter `verify(..., scope: "solution")` (`pass`, `10.0`, `0`).
- Abweichungen/Blockaden: Keine. Der Browser-Smoke benötigt wie alle Browser-Smokes manuell bereitgestellte Browser-/Datenbankvoraussetzungen; ohne diese wird er als Preflight-Fehler dokumentiert.
