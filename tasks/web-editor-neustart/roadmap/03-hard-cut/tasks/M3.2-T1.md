# M3.2-T1 – Routeübergreifende Browser-Abnahme und Ist-Doku

## Intention

Das Fundament ist erst fertig, wenn die vollständigen Nutzerabläufe,
gemeinsamen Seitenregeln und die spätere Erweiterbarkeit real geprüft sind.

## Voraussetzungen und Referenzen

- M3.1-T1 abgeschlossen; [Konzept](../../../Konzept.md),
  `.agents/rules/WebUiHtmlCss.mdc`, `docs/Manuelle-UI-Abnahme.md`.

## Scope und Nicht-Ziele

- **Verbindlich:** Browser-/Host-Nachweise der finalen Routen und Zustände
  gegen realen Server und realistischen tiefen Baum schließen:
  Lesen, Editieren, Dirty, Entwurf, leerer Baum, Drop-Erfolg/-Fehler,
  Validierung, Commit, Discard, Konflikt, Direktaufruf und Reload. Gleiche
  Innenkanten, Header/`h1`, ein `main`, Fokus und Skip-Link auf allen Routen
  bei 1280 × 720, 1024 × 720 sowie 200/400 % Zoom prüfen. Die Shell muss
  Platz für weitere Navigationsziele lassen, ohne Tree oder Dokument zu
  verschachteln. Erkennbare Verstöße in diesem Scope beheben.
- **Verbindlich:** `docs/` beschreibt nur den final implementierten
  Ist-Zustand (Routen, Arbeitsfluss, Layout, Exportentfall); Web-UI-Regeln
  und manuelle Abnahme passen dazu. Keine blinde Screenshot-Baseline-
  Aktualisierung.
- **Nicht:** neue Features, Pixelperfektion außerhalb des gemeinsamen
  Layoutvertrags, neue Produktentscheidungen oder Code außerhalb des
  Web-/Test-/Dokumentationsscopes.

## Akzeptanz und Testbudget

- [x] Browser-E2E belegen Lesen, Editieren, Dirty-Schutz, Entwurf, leeren Baum,
      erfolgreichen und abgelehnten Drop, Validierung, Commit, Discard, Konflikt,
      Direktaufruf und Reload gegen den realen Host. Gemeinsame Semantik,
      Innenkanten, Reflow und Aktionen sind für 1280, 1024, 640 und 320 CSS-Pixel
      automatisiert geprüft; Screenshots der finalen Routen wurden gemeinsam
      visuell geprüft.
- [ ] Menschliche Chrome-Zoom-Abnahme bei echtem 200/400-%-Zoom und menschliche
      Wahrnehmungsprüfung der Fokus-Sichtbarkeit gemäß
      [Manuelle UI-Abnahme](../../../../../docs/Manuelle-UI-Abnahme.md). Agenten-
      und Screenshotprüfungen ersetzen diese explizit menschlichen Schritte nicht.
- [x] Build, vollständige FastTests, Host-/HTTP-Integration,
      Browser-E2E, Solution-Linter-Gate, Ist-Doku-Abgleich und Diff-Prüfung grün.
- [x] Keine alte Web-Paralleloberfläche; die Konzept-Verifikationspunkte sind
      automatisiert nachgewiesen oder durch das oben benannte menschliche Gate
      abgegrenzt.

## Checkliste und Abschlussnachweis

- [x] Ist-Stand, Status und vorhandene Nachweise geprüft.
- [x] Scope umgesetzt, Nicht-Ziele eingehalten.
- [x] Automatisierte Akzeptanz, Tests, Build, Linter, Browser und Doku belegt.
- [ ] Menschliche Chrome-Zoom- und Fokuswahrnehmungs-Abnahme offen; M3.2 und
      M3 bleiben deshalb bis zu diesem Nachweis offen.
- [ ] Nachweise/Abweichungen hier; Parent-Checkbox nach Abnahme geschlossen;
      atomarer Commit erstellt.

Nachweis:

- `dotnet test` gegen veröffentlichten Server/Google Chrome Stable 153:
  Browser-Smokes für finale Routen und Arbeitsabläufe. `PageFrameSmokeTests`
  prüft gültigen ausgewählten Node und gültigen offenen Draft-Detail jetzt
  zusätzlich bei 1280 × 720, 1024 × 720 sowie 640/320 CSS-Pixeln. Dabei werden
  Innenkanten, genau ein `main`/`h1`, Seiten-Overflow, Diff, Validierung,
  Abschlussaktionen und der vollständige Shell-Link „Entwurf öffnen“ geprüft.
- Ein echter paralleler Current-Commit während eine Wissensbaumansicht geöffnet
  ist belegt die Ablehnung eines anschließend gezogenen veralteten Drops: die
  Fehlermeldung erscheint, es wird kein Entwurf begonnen, die bestätigte
  Geschwisterreihenfolge bleibt erhalten und ein Reload zeigt den aktuellen
  Serverbaum. Die temporär ergänzte Test-Node wird nach dem Browserlauf entfernt.
- `DraftWorkflowSmokeTests` ruft die Servervalidierung eines offenen Drafts auf
  und prüft das erfolgreiche Validierungsergebnis; derselbe Browserablauf belegt
  danach Discard und dessen Abwesenheit nach Reload. Commit und Konflikt/Reapply
  sind durch `SnapshotConflictSmokeTests` belegt.
- Gemeinsamer Screenshotlauf: `scripts/capture-ui-audit.ps1`, 15 Chrome-Aufnahmen
  der Knowledge-/Draft-Routen und Zustände. Die gültige Node- und Draft-Detail-
  Ansicht bei 1280 × 720, 1024 × 720 sowie 640/320 CSS-Pixeln wurde gemeinsam
  geprüft. Es zeigte sich eine auf 320 CSS-Pixeln abgeschnittene aktive
  Entwurfsaktion. Die Beschriftung wurde auf „Entwurf öffnen“ gekürzt und die
  Ellipsis-Begrenzung entfernt; der sichtbare Link wird nun auch schmal vollständig
  dargestellt. Es gab kein blindes Baseline-Update.
- Abschließende Gates: `scripts/build.ps1` erfolgreich; `scripts/test-fast.ps1`
  mit 1.004 Tests erfolgreich; Host-/HTTP-Integration 75/75 und vollständige
  Browser-Integration 36/36 erfolgreich; AiNetLinter `scope=solution` meldet
  `score=10.0`, null Verstöße; `git diff --check` ohne Befund.
- Im ersten vollständigen Browserlauf schlug einmal der bestehende After-Drop-
  Ablauf sporadisch vor dem Drag-Event fehl; isolierte Moves waren 4/4 grün und
  der vollständige Browserlauf danach 36/36 grün. Kein Produktionsfehler oder
  belastbarer reproduzierbarer Produktfehler war erkennbar.
- `docs/WebUi.md` nennt die abschließenden E2E-Nachweise pro Route und grenzt
  automatisierten Reflow von echter Zoom-/Fokuswahrnehmungsprüfung ab.
  `docs/Manuelle-UI-Abnahme.md` verweist für `/` auf den aktuellen
  `KnowledgeRedirectSmokeTests`.
- Verbleibendes manuelles Gate: Chrome Stable interaktiv auf echten 200/400 %
  stellen und Tastaturfokus-Sichtbarkeit menschlich prüfen. Gemäß
  `docs/Manuelle-UI-Abnahme.md` führt der Agent diese Punkte nicht interaktiv aus;
  sie sind bewusst nicht als belegt markiert.
