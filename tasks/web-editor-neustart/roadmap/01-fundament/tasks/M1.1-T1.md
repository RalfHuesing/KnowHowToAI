# M1.1-T1 – Regeln und Doku-Grenze

## Intention

Der neue Task muss vor Codearbeit ohne widersprüchliche Pflichten ausführbar
sein. Dies ist ein reiner Regel-/Dokumentations-Slice, keine UI-Implementierung.

## Voraussetzungen und Referenzen

- [Konzept](../../../Konzept.md), [Workflow](../../../../../.agents/agent-workflow/README.md),
  [Richtlinien](../../../../../.agents/rules/Richtlinien.mdc),
  [Doku-Regeln](../../../../../.agents/rules/DokuRichtlinien.mdc),
  [Web-UI-Regeln](../../../../../.agents/rules/WebUiHtmlCss.mdc).

## Scope und Nicht-Ziele

- **Verbindlich:** Verweise auf das gelöschte `tasks/webfrontend/` und die
  Aussage „Konzept/Roadmap existiert nicht“ in den genannten Regel- und
  Einstiegsdokumenten durch korrekte Bezüge zum Drei-Schritte-Workflow und
  diesem Task ersetzen. `docs/` bleibt ausschließlich Ist-Dokumentation.
- **Nicht:** Zukünftiges UI-Verhalten als Ist-Zustand dokumentieren, Code
  ändern oder alte Fachregeln aufweichen.

## Akzeptanz und Testbudget

- [x] Keine verbindliche Regel verlangt den nicht existierenden alten Task.
- [x] Task-Konzept/Roadmap und `docs/` sind als Soll-/Ist-Quellen klar getrennt.
- [x] `git diff --check` und Diff-Prüfung ohne Build/Test; reine Dokuänderung.

## Checkliste und Abschlussnachweis

- [x] Status und betroffene Dateien vor Änderung geprüft.
- [x] Scope umgesetzt und Akzeptanz belegt.
- [x] Nachweis, geänderte Dateien und Abweichungen hier dokumentiert.
- [x] Zugehörige Checkbox in `../roadmap.md` erst nach Abnahme geschlossen;
      atomarer Commit nur mit diesem Slice erstellt.

Nachweis:

- Geänderte Dateien: `.agents/rules/Richtlinien.mdc`,
  `.agents/rules/DokuRichtlinien.mdc`, `.agents/rules/WebUiHtmlCss.mdc`,
  `docs/README.md`, `docs/Konfiguration-und-Betrieb.md`,
  `tasks/web-editor-neustart/roadmap/01-fundament/roadmap.md` und dieser Leaf.
- Prüfung: Suche nach alten `tasks/webfrontend/`-Verweisen und der
  Nicht-Existenz-Aussage in den betroffenen Regeln und Einstiegstexten ohne
  verbleibende Fundstelle; `git diff --check` ohne Befund; Diff auf Scope und
  Links geprüft. Build und Tests entfallen gemäß Testbudget (reine
  Dokumentationsänderung).
- Abweichungen: Die technische Betriebsdokumentation enthielt ebenfalls einen
  veralteten Verweis auf das gelöschte Strukturkonzept. Er wurde durch den
  zuständigen allgemeinen Testregel-Einstieg ersetzt. Keine Code- oder
  Ist-Verhaltensänderungen.
- Commit: `docs: Web-Editor-Regel- und Dokuquellen aktualisieren`.
