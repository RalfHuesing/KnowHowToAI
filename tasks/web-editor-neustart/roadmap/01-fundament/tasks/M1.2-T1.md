# M1.2-T1 – Entwurfs- und URL-Arbeitszustand

## Intention

Baum und Node-Editor erhalten einen gemeinsamen, testbaren Web-Schreibweg:
erster Write startet genau einen Entwurf, weitere Writes verwenden ihn.

## Voraussetzungen und Referenzen

- M1.1-T1 abgeschlossen; [Konzept](../../../Konzept.md), Ist-Doku
  `docs/Transaktionen-und-Historie.md`, `docs/Zielgruppen-und-Content.md`.
- Ausgangspunkt: `Web/State/WorkspaceState.cs`,
  `Web/State/WebReadContextResolver*`, `Web/WebServiceRegistration.cs` und
  bestehende Application-Services für Snapshot/Transaction.

## Scope und Nicht-Ziele

- **Verbindlich:** Eine schlanke Koordination unter `Web/Workflow/` kapselt
  Begin/Resume/Write für Web-Aktionen. Sie setzt Actor aus
  `ICurrentUserService`/`DummyCurrentUserService`, `Client = "Web UI"` und
  `Purpose = "Wissenspflege"` ohne Formular, serialisiert konkurrierende
  erste Writes pro Arbeitsbereich,
  vergleicht vor Begin den geladenen Current Snapshot mit dem aktuellen und
  gibt `TransactionId` nach erfolgreichem Begin auch bei gescheiterter
  anschließender Mutation an die UI zurück. Existing-Transaction-Writes
  starten nie eine zweite Transaction.
- **Verbindlich:** `audienceId` und `transactionId` in der Node-URL sowie
  `/drafts/{TransactionId}` sind maßgeblich; `WorkspaceState` darf diese
  Information nur flüchtig spiegeln. Letzte Zielgruppe wird nur bei fehlendem
  URL-Wert eingesetzt; ein fremder Entwurf nie still gewählt. Fehler-/Dirty-
  Zustände bleiben für die UI unterscheidbar.
- **Nicht:** Editor-/Baumansichten, neues Datenmodell, Auth, Merge/Rebase oder
  zweite fachliche Transaction-Implementierung.

## Akzeptanz und Testbudget

- [x] FastTests belegen ersten Write, weiteren Write, parallelen ersten
      Write, veralteten Current ohne Mutation, Begin-Erfolg mit Mutation-Fehler
      und Reload/URL-Auswahl einschließlich fehlender Zielgruppe.
- [x] Core-Invarianten Current vs. Working Snapshot und explizite
      `TransactionId` bleiben erhalten; passende Ist-Doku mit Code ergänzt.
- [x] Build und vollständige FastTests für den Code-Slice ausgeführt; keine
      Browser-E2E-Variante für jede Koordinator-Verzweigung nötig.
- [x] AiNetLinter `verify(targetPath, scope: "solution")` mit Pass/10.0/0
      belegt.

## Checkliste und Abschlussnachweis

- [x] Ist-Stand, Status und kleinsten Baseline-Test geprüft.
- [x] Scope implementiert, Nicht-Ziele eingehalten.
- [x] Akzeptanzfälle, FastTests und Build belegt; Doku aktualisiert.
- [x] Nachweise und Abweichungen hier dokumentiert; atomarer Commit erstellt.
- [x] Parent-Checkbox erst nach erfolgreichem Linter-Gate geschlossen.

Nachweis:

- Ausgangsstand: sauberer Worktree; Baseline `dotnet test
  tests/KnowHowToAI.Web.Tests/KnowHowToAI.Web.Tests.csproj --no-restore
  --filter "FullyQualifiedName~WorkspaceStateTests|FullyQualifiedName~WebReadContextResolverTests"`
  grün (19 Tests).
- Umsetzung: `Web/Workflow/WebWriteCoordinator` delegiert Begin und Mutation
  an `TransactionService` beziehungsweise den übergebenen Application-Use-Case,
  serialisiert den ersten Begin pro Circuit, prüft Current-Freshness vor und
  nach Begin, setzt die festen Auditwerte und aktualisiert die Wissens-URL vor
  der Mutation. Die Transaction-ID bleibt auch bei Mutationsfehlern im Ergebnis
  und in der URL. `WorkspaceState` spiegelt den geladenen Snapshot und den aus
  der URL ausgewählten Kontext flüchtig; ein fehlerhafter URL-Kontext übernimmt
  nicht still den zuvor aktiven Circuit-Kontext.
- Tests: `WebWriteCoordinatorTests` prüfen ersten und weiteren Write,
  konkurrierenden Begin, stale Current ohne Mutation und Mutation-Fehler nach
  Begin samt Dirty-Erhalt. Resolver-/Page-FastTests prüfen Transaction-Reload
  aus `transactionId`, Snapshotkennung und fehlende Zielgruppe.
- Linter-Fixes: Das erste Solution-Gate fand zwei Fehler in
  `WebWriteCoordinator.WriteAsync` (kognitive Komplexität 16/15, 86/60
  Codezeilen); Auswahl und Mutationslauf wurden getrennt. Die weiteren
  Befunde wurden an ihren Ursachen behoben: `WorkspaceState` enthält keine
  redundanten öffentlichen `IsDirty`-/`HasActiveTransaction`-Aliase mehr;
  `TransactionPage` delegiert Commit, Discard und Konfliktbehandlung an die
  kohärente `TransactionCompletionPanel`. Die geplanten alten Routen bleiben
  bis M3 erhalten. Keine Suppression, Regeländerung, Integration oder
  Konfiguration ergänzt.
- Gates: `pwsh -NoProfile -File scripts/build.ps1` erfolgreich;
  `pwsh -NoProfile -File scripts/test-fast.ps1` erfolgreich (Frontend 5,
  Analyzer 6, Core 419, Integration-Projekt-FastTests 286, Web 419).
  Zusätzlich gezielter `TransactionLifecycleTests`-Lauf: 7/7 bestanden.
  `git diff --check` ohne Befund.
- Solution-Gate: `verify(targetPath, scope: "solution")` vollständig
  bestanden: Pass, Score 10.0, 0 Verstöße.
- Commit: `refactor: Web-Transaktionszustand und Abschlussfluss schärfen`.
- Integrationstests mit SQL/Host/Browser wurden nicht separat gestartet: dieser
  Slice ändert nur die Web-Anwendungskoordination und nutzt die vorhandenen
  Application-Use-Cases. Die FastTest-Suite umfasst das Integrationstestprojekt
  in dessen deterministischer Unit-Kategorie.
