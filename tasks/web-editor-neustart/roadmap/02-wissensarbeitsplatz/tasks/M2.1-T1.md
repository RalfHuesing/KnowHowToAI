# M2.1-T1 – Baum und Dokumentansicht

## Intention

Wissensnavigation und Node-Lesen werden zur Hauptaufgabe der Seite, ohne
einen ganzen großen Baum auf einmal zu laden.

## Voraussetzungen und Referenzen

- M1 abgenommen; [Konzept](../../../Konzept.md).
- Ausgangspunkt: `Web/Features/Knowledge/{KnowledgePage,KnowledgeTree,
  KnowledgeTreeState,KnowledgeTreePageCache,KnowledgeTreePathLoader,
  KnowledgeTreeRequestCoordinator,NodeDetails,SafeMarkdownRenderer}*`.

## Scope und Nicht-Ziele

- **Verbindlich:** Wissensseite unter `/knowledge` und
  `/knowledge/{NodeId:guid}` als kontextuellen Baum plus Node-Dokument
  zusammensetzen. Baum lädt astweise mit bestehender Paging-/Pfadmechanik,
  markiert Auswahl und hält den sichtbaren Pfad bei Direktaufruf/Reload.
  Ohne Node: Auswahlaufforderung; ohne Root: klare Erstanlage-Aktion, deren
  Mutation M2.3 umsetzt. Node zeigt Titel als einziges `h1`, Beschreibung,
  sicheren Markdown-Inhalt und sichtbare Zielgruppe/Provenienz. Eine einzige
  Zielgruppe wird sichtbar gewählt; bei mehreren ohne URL-/letzte Auswahl
  keine stille Content-Auswahl. Current und expliziter `transactionId`
  laden den richtigen ReadContext.
- **Verbindlich:** `Tree/` und `Node/` besitzen Feature-Flächen; kein zweites
  `main`, keine eigene Seiten-Außenbreite. Vorhandene Loader/Renderer werden
  nach Vertrag genutzt; nur notwendige Namespace-/Ordnerbewegungen.
- **Nicht:** Mutations-UI, Suche, kompletter Baum-Dump, Shell-Fachlogik.

## Akzeptanz und Testbudget

- [x] FastTests: leerer Baum, tiefer Pfad, Paging ohne Duplikate,
      Node-URL/Reload, Current vs. Working, Zielgruppenwahl und Fallback-
      Anzeige; keine Schreibaktion beim bloßen Lesen.
- [x] Browser-Nachweis für Navigation in realistischem tiefem Baum und
      direkt geladene Node; Projekt-Quality-Gates und Ist-Doku abgeschlossen.

## Checkliste und Abschlussnachweis

- [x] Ist-Stand, Status und Baseline geprüft.
- [x] Scope implementiert, Nicht-Ziele eingehalten.
- [x] Akzeptanz, Tests, Build, Linter und Doku belegt.
- [x] Nachweise/Abweichungen hier; Parent-Checkbox nach Abnahme geschlossen;
      atomarer Commit erstellt.

Nachweis:

- `/knowledge` und `/knowledge/{NodeId:guid}` verwenden den paginierten,
  kontextuellen Lesebaum und eine separate schreibgeschützte Dokumentansicht.
  Der aktive Vertrag unterstützt Current und explizite `transactionId`; die
  bisherigen Bearbeitungsbausteine und ihre M2.2/M2.3-Tests bleiben erhalten.
- Vollständige FastTests: 1.137 bestanden (Frontend 5, Analyzer 6, Core 419,
  Integration 286, Web 421). Knowledge-Browser-Smokes: 2/2 bestanden,
  einschließlich vierstufigem Direktpfad, Reload und Breadcrumb-Wiederherstellung.
- UI-Audit: 22/22 Desktop-Aufnahmen bei 1280 × 800 erfolgreich; Knowledge-
  Root-, Zielgruppen-, Node-, Fallback- und Working-Ansichten visuell geprüft.
  Manifest und Screenshots: `temp/ui-audit-m2.1/2026-09-23_00-57-21/`.
- `scripts/build.ps1`: Exitcode 0. Solution-Linter: Score 10.0, 0 Verstöße.
  `docs/WebUi.md` und `docs/Architektur.md` aktualisiert.
- Der frühere Seitentest `NodeMutationSynchronizationTests` entfällt mit der
  alten Mutations-UI auf `/knowledge`. M2.2 muss den
  Routensynchronisationsnachweis für die neue Bearbeitungsfläche ergänzen;
  Komponenten- und Use-Case-Tests der Bearbeitungsfunktionen bleiben bestehen.
- M2.1-T1 ist abgenommen und in `roadmap.md` geschlossen; der M2-Meilenstein
  bleibt wegen M2.2 und M2.3 offen.
