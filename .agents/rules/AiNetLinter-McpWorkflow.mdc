---
description: Verbindlicher MCP-First Workflow für C#/.NET-Codeanalyse mit AiNetLinter.
globs: "**/*.cs"
alwaysApply: false
---

# AiNetLinter MCP-First Workflow (C# Semantik)

Bei C#-Aufgaben (Code-Erkundung, Symbole, Methoden, Aufrufer, Hierarchien, Impact, Linter) **immer pro-aktiv** die semantischen MCP-Tools des für das aktuelle Projekt konfigurierten AiNetLinter-Servers anstelle manueller Textsuch-Kaskaden (`rg` + seitenweises Dateilesen) verwenden. Sie liefern präzisen AST-Kontext in 1 Turn.

## Werkzeug-Wahl & Fallback

- **Wann `rg` / lokale Textsuche sinnvoll und erlaubt ist:**
  - `rg` ist keineswegs verboten, sondern eine wertvolle Ergänzung.
  - **Nicht-C#-Dateien:** JSON, YAML, `.csproj`, Markdown, Scripts, Configs, Logs.
  - **Reine Textsuche:** Exakte String-Literale, Fehlermeldungen, Kommentare.
  - **Pragmatischer Fallback:** Wenn ein MCP-Tool fehlschlägt, keine Treffer liefert oder unvollständig ist.
  - **Gezielte Edits:** Wenn der exakte Zeilenbereich bereits bekannt ist.

## Standard-Parameter & Handoff

- **`targetPath` (Pflicht)**: Absoluter Pfad zur `.sln`- oder `.slnx`-Datei des aktuellen Projekts (einmalig im Workspace ermitteln und für alle Aufrufe wiederverwenden).
- **Folgeaufrufe & Handoff**: Ausschließlich die im Content als Handoff-ID ausgewiesenen kanonischen IDs (z. B. von `find_symbol`, `get_file_skeleton`, `get_feature_context`) verwenden – niemals Anzeigenamen erraten oder aus freien Beschreibungen ableiten. Status und Vollständigkeitsmarker ebenfalls ausschließlich aus dem Content übernehmen.
- **Budget-Limit**: Bei Fehler `RESPONSE_BUDGET_TOO_SMALL` denselben Aufruf mit `maxResponseBytes = minimumResponseBytes` wiederholen.
- **Qualitätsprüfung**: Während Änderungen `verify(targetPath)` ohne zusätzliche Scope-, Filter-, Schwellenwert- oder Detektorparameter aufrufen. Für den vollständigen Abschluss ausschließlich `verify(targetPath, scope: "solution")` verwenden; ein Gate ist nur bei `verdict=pass`, `score=10.0` und `violationCount=0` erfüllt.

## Verstöße architektonisch beheben

- Jeder Verstoß ist zuerst ein Signal für eine mögliche Verantwortungs-, Abhängigkeits- oder Strukturgrenze. Vor einer Änderung mit `get_feature_context` und den zum Befund passenden Graph-/Impact-Tools Ursache, Kontextpfade, Aufrufer und Tests ermitteln.
- Die kleinste Lösung wählen, die eine fachlich kohärente Verantwortung oder Grenze wiederherstellt; vorhandene Bausteine bevorzugen. Neue Komponenten, Typen oder Facades nur bei einer belegbaren, eigenständigen Verantwortung – nicht als bloße Indirektion.
- Keine lokale Umgehung: keine Regel-/Schwellenwert-Ausnahmen, Suppressions, kosmetischen Verschiebungen oder sachfremden Refactorings allein für eine Kennzahl. Verhalten, Verträge und Tests bleiben erhalten; Tests folgen einer tatsächlich neuen Grenze.

---

## Workflow & Tool-Referenz

| Kategorie | Ziel | MCP-Tool (`AiNetLinter`) | Wichtigste Parameter & Hinweise |
|---|---|---|---|
| **Erkundung & Code** | **Einstieg / Kontext** | `get_feature_context` | `symbolIdentifier: "..."` (Signatur, Members, Aufrufer, Tests & Violations in 1 Call) |
| | **Symbol finden** | `find_symbol` | `pattern: "..."`, optional `kind: "class"\|"method"\|...` |
| | **Code lesen** | `get_symbol_body` | `symbolIdentifiers: ["..."]` (exakter Rumpf ohne Datei-Overhead) |
| | **Datei-Skelett** | `get_file_skeleton` | `filePath: "..."` (Typen und Member-Signaturen ohne Rümpfe) |
| | **Klassenstruktur** | `get_class_structure` | `symbolIdentifier: "..."` (Detaillierte Member-Übersicht) |
| **Graphen & Hierarchie** | **Aufrufbaum** | `get_call_tree` | `symbolIdentifier: "..."`, `direction: "incoming"\|"outgoing"` |
| | **Hierarchie / Impl.** | `get_type_hierarchy` / `find_implementations` | `symbolIdentifier: "..."` |
| | **Impact vor Änderung** | `get_impact` | `symbolIdentifier: "..."` (betroffene Aufrufer & relevante Tests) |
| | **Referenzen** | `find_references` | `symbolIdentifier: "..."` (alle Verwendungsstellen) |
| **Qualität & Analyse** | **Qualitätsprüfung** | `verify` | `changes` (Default während Arbeit); `solution` als vollständiger Abschlussnachweis |
| | **Hotspots & Duplikate** | `get_hotspots` / `find_duplicates` | Ermittelt Problemzonen und Code-Duplizierung |
| | **Mustererkennung** | `pattern_detect` / `search_pattern` | Mustersuche auf AST-Ebene bzw. Regex über C#-Code |
| | **Metriken** | `metrics_lookup` / `metrics_tree` | Komplexitäts- und Qualitätsmetriken abfragen |
| **Projekt & Assembly** | **Projektstruktur** | `get_file_tree` | `fileFilter: "*.cs"`, `view: "summary"` (*nie `view="files"` auf Root!*) |
| | **Namespaces & Graph** | `get_namespace_tree` / `dependency_graph` | Namespace-Hierarchie und Komponentenabhängigkeiten |
| | **Assemblies (.dll)** | `inspect_assembly` / `search_assembly` | `targetPath: "<pfad.dll>"` (Metadaten, Typen & API dekompilieren) |
| | **Assembly-Extensions**| `find_assembly_extensions` | Extension Methods in kompilierter Assembly finden |
| | **Status & Health** | `get_server_health` / `reload_config` | Serverzustand prüfen / Konfiguration dynamisch neu laden |
